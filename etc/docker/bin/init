#!/bin/bash

docker-compose run php bin/console cache:clear
docker-compose run php bin/console doctrine:migrations:migrate --no-interaction
docker-compose run php bin/console h:d:f:l --no-interaction
