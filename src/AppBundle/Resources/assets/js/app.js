'use strict';

$ = window.$ = window.jQuery = require('jquery');
var domready = require('domready');
require('bootstrap');

var App = {
    init: function() {

    }
};

domready(function() {
    App.init();
});
